// Kept in sync by hand with --background-deep in src/app/globals.css.
// Canvas 2D cannot read CSS custom properties directly, so this raw hex
// is the canvas-safe mirror of that token - update both together.
export const BACKGROUND_DEEP_HEX = "#030308";
